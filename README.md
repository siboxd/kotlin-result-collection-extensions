# kotlin-result-collection-extensions
This project adds several new extensions to collection types containing the Kotlin Result (`kotlin.Result&lt;T>`) type to facilitate more robust, functional-style mapping and error handling.
